# Required for /bengpt (app/api/bengpt/chat). Get a key from
# https://console.anthropic.com/ and, importantly, set a hard monthly spend
# cap on it in the Console — that's the real backstop against runaway cost,
# not the in-app rate limiter.
ANTHROPIC_API_KEY=
